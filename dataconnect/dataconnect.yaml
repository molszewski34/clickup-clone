specVersion: "v1beta"
serviceId: "clickup-clone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "clickup-clone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
